#include <stdio.h>
int main () {
    int a, b, c, d, ortaarifmetik = 0;
    scanf("%d", &a);
    scanf("%d", &b);
    scanf("%d", &c);
    scanf("%d", &d);
    ortaarifmetik = (a + b + c + d) / 4;
    printf("%d", ortaarifmetik);

    return 0;
}